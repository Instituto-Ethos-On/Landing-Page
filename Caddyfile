ethoson.com.br, www.ethoson.com.br {
    root * /var/www/public
    php_fastcgi ethoson-site:9000
    encode gzip
    file_server
    log {
        output file /var/log/caddy/access.log {
            roll_size 100mb
            roll_keep 5
            roll_keep_for 48h
        }
    }
}
